# This function will run a naive univariate particle Gibbs sampler.
# 
# Input:
#   y  : data
#   od : observation density (mass)
#   er : evolution sampling
#   gs : parameter sampler
#   J  : number of particles
#   in.x : initial values
#
pgs <- function(y, od, er, gs, J, in.x) {
  n = length(y)
  x = in$x
  w = 
}